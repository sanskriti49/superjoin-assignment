fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
sqlalchemy>=2.0.0
pypdf>=4.0.0
python-multipart>=0.0.9
pytest>=8.0.0
httpx>=0.27.0

# Only needed if DATABASE_URL points at PostgreSQL. SQLite is the default and
# needs nothing installed.
# psycopg2-binary>=2.9.9
